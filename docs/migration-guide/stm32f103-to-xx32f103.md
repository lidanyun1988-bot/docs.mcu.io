---
sidebar_position: 1
---

# 从 STM32F103 迁移到 XX32F103

:::tip 本页摘要
STM32F103C8T6 → XX32F103C8T6 引脚兼容迁移指南。涵盖时钟、GPIO、USART、SPI、I2C、ADC 等外设的逐项对比和代码差异说明，100% 兼容无需修改代码。
:::

## 兼容性总览

XX32F103 与 STM32F103 **100% 兼容**，可直接替换使用。

| 项目 | STM32F103 | XX32F103 | 兼容性 |
|------|-----------|----------|--------|
| 内核 | Cortex-M3 | Cortex-M3 | ✅ 相同 |
| 主频 | 72MHz | 72MHz | ✅ 相同 |
| Flash | 64-512KB | 64-512KB | ✅ 相同 |
| SRAM | 20-96KB | 20-96KB | ✅ 相同 |
| 引脚 | 兼容 | 兼容 | ✅ Pin-to-Pin |

## 引脚对比

### XX32F103C8T6 vs STM32F103C8T6

| 功能 | STM32F103 | XX32F103 | 说明 |
|------|----------|---------|------|
| PA0 | GPIO / ADC_IN0 | GPIO / ADC_IN0 | ✅ 兼容 |
| PA1 | GPIO / ADC_IN1 | GPIO / ADC_IN1 | ✅ 兼容 |
| PA2 | GPIO / USART2_TX | GPIO / USART2_TX | ✅ 兼容 |
| PA3 | GPIO / USART2_RX | GPIO / USART2_RX | ✅ 兼容 |
| ... | ... | ... | ✅ 全部兼容 |

**结论**: 所有引脚定义完全相同，可直接替换。

## 时钟配置

### 时钟系统对比

| 时钟源 | STM32F103 | XX32F103 | 说明 |
|--------|-----------|----------|------|
| HSI | 8MHz | 8MHz | ✅ 相同 |
| HSE | 4-16MHz | 4-16MHz | ✅ 相同 |
| PLL | ×2~×16 | ×2~×16 | ✅ 相同 |
| LSI | ~40kHz | ~40kHz | ✅ 相同 |
| LSE | 32.768kHz | 32.768kHz | ✅ 相同 |

### 代码示例（完全相同）

```c
/* STM32F103 和 XX32F103 使用相同的时钟配置代码 */
RCC_OscInitTypeDef RCC_OscInitStruct = {0};
RCC_ClkInitTypeDef RCC_ClkInitStruct = {0};

RCC_OscInitStruct.OscillatorType = RCC_OSCILLATORTYPE_HSE;
RCC_OscInitStruct.HSEState = RCC_HSE_ON;
RCC_OscInitStruct.HSEPredivValue = RCC_HSE_PREDIV_DIV1;
RCC_OscInitStruct.PLL.PLLState = RCC_PLL_ON;
RCC_OscInitStruct.PLL.PLLSource = RCC_PLLSOURCE_HSE;
RCC_OscInitStruct.PLL.PLLMUL = RCC_PLL_MUL9;
HAL_RCC_OscConfig(&RCC_OscInitStruct);

RCC_ClkInitStruct.ClockType = RCC_CLOCKTYPE_HCLK | RCC_CLOCKTYPE_SYSCLK
                            | RCC_CLOCKTYPE_PCLK1 | RCC_CLOCKTYPE_PCLK2;
RCC_ClkInitStruct.SYSCLKSource = RCC_SYSCLKSOURCE_PLLCLK;
RCC_ClkInitStruct.AHBCLKDivider = RCC_SYSCLK_DIV1;
RCC_ClkInitStruct.APB1CLKDivider = RCC_HCLK_DIV2;
RCC_ClkInitStruct.APB2CLKDivider = RCC_HCLK_DIV1;
HAL_RCC_ClockConfig(&RCC_ClkInitStruct, FLASH_LATENCY_2);
```

## GPIO

### 寄存器对比

| 寄存器 | STM32F103 | XX32F103 | 地址偏移 |
|--------|-----------|----------|---------|
| CRL | 0x00 | 0x00 | ✅ 相同 |
| CRH | 0x04 | 0x04 | ✅ 相同 |
| IDR | 0x08 | 0x08 | ✅ 相同 |
| ODR | 0x0C | 0x0C | ✅ 相同 |
| BSRR | 0x10 | 0x10 | ✅ 相同 |

### 代码示例（无需修改）

```c
/* GPIO 初始化代码完全相同 */
GPIO_InitTypeDef GPIO_InitStruct = {0};
__HAL_RCC_GPIOA_CLK_ENABLE();

GPIO_InitStruct.Pin = GPIO_PIN_5;
GPIO_InitStruct.Mode = GPIO_MODE_OUTPUT_PP;
GPIO_InitStruct.Pull = GPIO_NOPULL;
GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_LOW;
HAL_GPIO_Init(GPIOA, &GPIO_InitStruct);
```

## USART

### 对比说明

| 特性 | STM32F103 | XX32F103 |
|------|-----------|----------|
| USART 数量 | 3 | 3 |
| 最高波特率 | 4.5Mbps | 4.5Mbps |
| FIFO | ❌ | ❌ |
| DMA 支持 | ✅ | ✅ |

### 代码示例（无需修改）

```c
/* USART 配置代码完全相同 */
UART_HandleTypeDef huart2;
huart2.Instance = USART2;
huart2.Init.BaudRate = 115200;
huart2.Init.WordLength = UART_WORDLENGTH_8B;
huart2.Init.StopBits = UART_STOPBITS_1;
huart2.Init.Parity = UART_PARITY_NONE;
huart2.Init.Mode = UART_MODE_TX_RX;
huart2.Init.HwFlowCtl = UART_HWCONTROL_NONE;
huart2.Init.OverSampling = UART_OVERSAMPLING_16;
HAL_UART_Init(&huart2);
```

## SPI

### 对比说明

| 特性 | STM32F103 | XX32F103 |
|------|-----------|----------|
| SPI 数量 | 2 | 2 |
| 最高速率 | 18Mbps | 18Mbps |
| 模式 | Master/Slave | Master/Slave |

## I2C

### 对比说明

| 特性 | STM32F103 | XX32F103 |
|------|-----------|----------|
| I2C 数量 | 2 | 2 |
| 最高速率 | 400kHz | 400kHz |
| SMBus | ✅ | ✅ |

## ADC

### 对比说明

| 特性 | STM32F103 | XX32F103 |
|------|-----------|----------|
| ADC 数量 | 2 | 2 |
| 分辨率 | 12-bit | 12-bit |
| 通道数 | 16 | 16 |
| 采样率 | 1Msps | 1Msps |

## 定时器

### 对比说明

| 定时器 | STM32F103 | XX32F103 | 功能 |
|--------|-----------|----------|------|
| TIM1 | ✅ | ✅ | 高级定时器 |
| TIM2 | ✅ | ✅ | 通用定时器 |
| TIM3 | ✅ | ✅ | 通用定时器 |
| TIM4 | ✅ | ✅ | 通用定时器 |

## 迁移验证清单

### 硬件验证

- [ ] 供电电压（2.0V~3.6V）
- [ ] 晶振频率（8MHz HSE）
- [ ] 复位电路
- [ ] 调试接口（SWD）

### 软件验证

- [ ] 编译无错误
- [ ] 下载成功
- [ ] 基本功能正常
- [ ] 外设功能正常

### 性能验证

- [ ] 主频测试（72MHz）
- [ ] 功耗测试
- [ ] 温度测试

## 常见问题

### Q: 迁移后需要修改代码吗？

A: **不需要**。XX32F103 与 STM32F103 完全兼容，代码无需任何修改。

### Q: 需要重新配置 STM32CubeMX 吗？

A: 可以直接使用 STM32F103 的.ioc 文件生成代码，或选择 XX32F103 对应型号。

### Q: 启动文件需要修改吗？

A: 不需要，启动文件完全相同。

## 总结

XX32F103 与 STM32F103 **100% 兼容**：

- ✅ 引脚定义完全相同
- ✅ 寄存器定义完全相同
- ✅ 外设功能完全相同
- ✅ 代码无需任何修改
- ✅ 开发工具完全兼容

**迁移成本**: 几乎为零

---

> 📝 [在 GitHub 上编辑此页](https://github.com/your-org/developer-hub-docs/edit/main/docs/migration-guide/stm32f103-to-xx32f103.md)
